db:
  host:
  user:
  password:
  dbname:
  port:
auth:
  secretKey:
logs:
  dirPath: /var/log/pokeRest/
  server:
    filename: server.log
    maxFileSizeMB: 200
    maxBackupNum: 5
    maxRetentionDays: 30
  access:
    filename: access.log
    maxFileSizeMB: 200
    maxBackupNum: 5
    maxRetentionDays: 30
  db:
    filename: db.log
    maxFileSizeMB: 200
    maxBackupNum: 5
    maxRetentionDays: 30
